package gui

import (
	l "gioui.org/layout"
)

func (ng *NodeGUI) Config() l.Widget {
	// schema := pod.GetConfigSchema(ng.cx.Config, ng.cx.ConfigMap)
	// tabNames := make(map[string]struct{})
	// var tabs :=  make(WidgetMap)
	// for i := range schema.Groups {
	// 	for j := range schema.Groups[i].Fields {
	// 		tabNames[schema.Groups[i].Fields[j].Group] = struct{}{}
	//
	// 		schema.Groups[i].Fields[j].Slug
	// 	}
	// }
	return func(gtx l.Context) l.Dimensions {


		return l.Dimensions{}
	}
}
